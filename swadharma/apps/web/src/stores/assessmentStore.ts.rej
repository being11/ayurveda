diff a/swadharma/apps/web/src/stores/assessmentStore.ts b/swadharma/apps/web/src/stores/assessmentStore.ts	(rejected hunks)
@@ -14,6 +14,7 @@ import {
 import type { AssessmentState, PanchakarmaTherapy } from '../types/assessment';
 import { computeProfile, getDominantDosha } from '../engines/report';
 import panchakarmaData from '../data/panchakarma.json';
+import seasonsData from '../data/seasons.json';
 
 const firstCategory = categories[0];
 const firstQuestion = firstCategory?.questions[0] ?? null;
@@ -35,6 +36,42 @@ const useAssessmentStore = create<AssessmentState>()(
 
       selectedSrotas: null,
 
+      currentSeasonId: null,
+
+      autoDetectSeason: () => {
+        const today = new Date();
+        const month = today.getMonth() + 1; // 1-12
+        const day = today.getDate();
+        
+        let foundSeason = seasonsData[0]?.id || 'vasanta';
+        
+        for (const season of seasonsData) {
+          const partsStart = season.startDate.split('-').map(Number);
+          const partsEnd = season.endDate.split('-').map(Number);
+          const startMonth = partsStart[0] ?? 1;
+          const startDay = partsStart[1] ?? 1;
+          const endMonth = partsEnd[0] ?? 12;
+          const endDay = partsEnd[1] ?? 31;
+          
+          let isMatch = false;
+          if (startMonth < endMonth) {
+             isMatch = (month > startMonth || (month === startMonth && day >= startDay)) && 
+                       (month < endMonth || (month === endMonth && day <= endDay));
+          } else {
+             // wraps around year
+             isMatch = (month > startMonth || (month === startMonth && day >= startDay)) || 
+                       (month < endMonth || (month === endMonth && day <= endDay));
+          }
+          if (isMatch) {
+            foundSeason = season.id;
+            break;
+          }
+        }
+        set({ currentSeasonId: foundSeason });
+      },
+
+      setCurrentSeasonId: (seasonId) => set({ currentSeasonId: seasonId }),
+
       setAnswer: (questionId, value) => {
         const state = get();
         const allQuestions = categories.flatMap(cat => cat.questions);
@@ -135,6 +172,7 @@ const useAssessmentStore = create<AssessmentState>()(
           herbDoshaFilter: null,
           herbOrganFilter: null,
           selectedSrotas: null,
+          currentSeasonId: null,
         });
         try {
           useAssessmentStore.persist.clearStorage();
