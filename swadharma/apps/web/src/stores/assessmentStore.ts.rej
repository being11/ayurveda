diff a/swadharma/apps/web/src/stores/assessmentStore.ts b/swadharma/apps/web/src/stores/assessmentStore.ts	(rejected hunks)
@@ -26,6 +26,9 @@ const useAssessmentStore = create<AssessmentState>()(
       history: firstQuestion ? [firstQuestion.id] : [],
       isComplete: false,
       activeNadiPoint: null,
+      herbSearchQuery: '',
+      herbDoshaFilter: 'all',
+      herbOrganFilter: 'all',
 
       setAnswer: (questionId, value) => {
         const state = get();
@@ -122,6 +125,9 @@ const useAssessmentStore = create<AssessmentState>()(
       },
       
       setActiveNadiPoint: (point) => set({ activeNadiPoint: point }),
+      setHerbSearchQuery: (q: string) => set({ herbSearchQuery: q }),
+      setHerbDoshaFilter: (f: string) => set({ herbDoshaFilter: f }),
+      setHerbOrganFilter: (f: string) => set({ herbOrganFilter: f }),
     }),
     {
       name: 'swadharma-assessment',
