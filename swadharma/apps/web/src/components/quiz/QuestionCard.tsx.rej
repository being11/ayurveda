diff a/swadharma/apps/web/src/components/quiz/QuestionCard.tsx b/swadharma/apps/web/src/components/quiz/QuestionCard.tsx	(rejected hunks)
@@ -16,6 +16,14 @@ export function QuestionCard({ question, currentAnswer = '', onAnswer, onOptionC
     if (onAnswer) onAnswer(optionId);
   };
 
+  // Determine if we should use a horizontal Likert scale layout
+  const isLikertScale = question.type === 'single' && question.options.some(opt => 
+    opt.label.toLowerCase().includes('agree') || 
+    opt.label.toLowerCase().includes('rarely') ||
+    opt.label.toLowerCase().includes('often') ||
+    opt.label.toLowerCase().includes('neutral')
+  ) && question.options.length <= 5;
+
   return (
     <AnimatePresence mode="wait">
       <motion.div
@@ -37,12 +45,36 @@ export function QuestionCard({ question, currentAnswer = '', onAnswer, onOptionC
           )}
         </div>
 
-        <div className="grid gap-3 pt-4">
+        <div className={cn(
+          "pt-4",
+          isLikertScale ? "flex flex-col sm:flex-row gap-3 sm:gap-2 justify-between w-full" : "grid gap-3"
+        )}>
           {question.options.map((option, index) => {
             const isSelected = Array.isArray(currentAnswer)
               ? currentAnswer.includes(option.id)
               : currentAnswer === option.id;
 
+            if (isLikertScale) {
+              return (
+                <motion.button
+                  key={option.id}
+                  whileHover={{ scale: 1.02 }}
+                  whileTap={{ scale: 0.98 }}
+                  onClick={() => handleOptionClick(option.id)}
+                  className={cn(
+                    "flex-1 flex flex-col items-center justify-center p-4 rounded-xl border-2 transition-all duration-200 min-h-[100px]",
+                    isSelected
+                      ? "border-[#4A7C59] bg-[#4A7C59]/10 shadow-sm"
+                      : "border-transparent bg-[#FBF8F2] hover:border-[#4A7C59]/30 hover:bg-white/80 shadow-sm"
+                  )}
+                >
+                  <span className="text-sm md:text-base font-medium text-center text-gray-800">
+                    {option.label}
+                  </span>
+                </motion.button>
+              );
+            }
+
             return (
               <motion.button
                 key={option.id}
@@ -85,4 +117,3 @@ export function QuestionCard({ question, currentAnswer = '', onAnswer, onOptionC
     </AnimatePresence>
   );
 }
-// trigger change
