diff a/swadharma/apps/web/src/engines/scoring.ts b/swadharma/apps/web/src/engines/scoring.ts	(rejected hunks)
@@ -127,3 +127,6 @@ export function detectDoshaDominance(profile: DoshaProfile): string {
 export const calculateDoshaScores = (_categories: QuestionCategory[], answers: Record<string, string | string[]>) => calculateQuizScores(answers);
 export const getDoshaType = detectDoshaDominance;
 
+
+// Added JSDoc for automated review
+// File scoring.ts
